{
  "name": "nemt-production-app",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "predeploy": "node --input-type=module -e \"import { rmSync } from 'node:fs'; rmSync('.wrangler', { recursive: true, force: true });\"",
    "deploy": "npm run predeploy && npm run build && npx wrangler deploy --config dist/server/wrangler.json",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.4",
    "astro": "^7.1.3",
    "jose": "^6.2.4",
    "wrangler": "^4.114.0"
  }
}
